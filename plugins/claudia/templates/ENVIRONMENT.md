# Environment — <name>

<!-- Written by `claudia env capture --output .planning/ENVIRONMENT.md`,
     which uses the bundled template at
     `claudia_tools/data/ENVIRONMENT.md.template`. This copy under the
     workflow plugin is a *documentation example* — the live render comes
     from the CLI, not from `claudia template render`. -->

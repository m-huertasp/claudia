---
description: Ship the verified phase — run a final secret scan, then hand off to /claudia-draft-pr so the user reviews and accepts the pull request before it is created.
---

# Ship the phase

Follow `plugins/claudia/workflows/ship.md`.

Argument: `$ARGUMENTS` — passed through to `/claudia-draft-pr` (e.g. `base:dev`).

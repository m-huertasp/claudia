# Verification — {{ name }}

<!-- Auto-managed by `claudia verify`. The Human checklist below tracks the
     human-run checks (e.g. full pipeline runs) that must clear before
     /claudia-close proceeds. Tick items with `claudia verify confirm V<N>`.
     The Automated results section is human-owned: /claudia-verify fills it
     when the automated tests run. -->

## Automated results

<Recorded by /claudia-verify when the automated tests run. Free text —
list the runners that executed, their verdicts, and any findings.>

## Human checklist

<!-- claudia:checklist -->

<!-- /claudia:checklist -->

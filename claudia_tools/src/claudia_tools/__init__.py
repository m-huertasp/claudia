"""claudia-tools — the deterministic engine for the claudia workflow.

This package owns every mechanical operation the claudia workflow needs —
parsing and updating planning artifacts, validating configuration, rendering
templates, and enforcing review gates — so the orchestrating model never has
to hand-edit those files.
"""

__version__ = "0.1.0"
